# P8.20 Shell Workspace Evidence Workbench Projection Plan

Work item: P8.20 Shell Workspace Evidence Workbench Projection Plan
Date: 2026-07-10
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: pass; the next safe slice may emit a deterministic WindowsUtility shell/workspace evidence workbench projection.

## Scope

P8.20 defines the projection boundary for the first WindowsUtility real-project workbench surface.

This slice is plan/report-only. It does not emit a workbench JSON projection, create HTML, launch the app, capture new screenshots, mutate WindowsUtility, apply proposals, grant AI authority, grant hardware authority, or claim productization readiness.

## Required Projection Inputs

The future projection must connect these already-recorded artifacts:

- accepted shell/workspace mapping: `generated/windowsutility/p8.9-shell-workspace-accepted-mapping.json`
- accepted mapping verification: `generated/windowsutility/p8.9-shell-workspace-accepted-mapping-verification-report.json`
- accepted mapping negative probes: `generated/windowsutility/p8.10-shell-workspace-accepted-mapping-negative-probes-report.json`
- non-applied smoke evidence proposal: `generated/windowsutility/p8.12-shell-workspace-smoke-evidence-non-applied-proposal.json`
- non-applied proposal validation: `generated/windowsutility/p8.12-shell-workspace-smoke-evidence-non-applied-proposal-validation-report.json`
- non-applied proposal negative probes: `generated/windowsutility/p8.13-shell-workspace-non-applied-proposal-negative-probes-report.json`
- sandboxed build evidence: `generated/windowsutility/p8.15-sandboxed-smoke-evidence-report.json`
- sandboxed UI launch evidence: `generated/windowsutility/p8.17-sandboxed-ui-launch-probe-report.json`
- sandboxed screenshot evidence: `generated/windowsutility/p8.19-sandboxed-screenshot-probe-report.json`
- screenshot PNG: `generated/windowsutility/p8.19-shell-workspace-sandboxed-window.png`

## Required Projection Sections

The future projection should expose:

- workflow timeline from mapping hypothesis to screenshot evidence
- accepted mapping card with source refs and target baseline
- proposal card showing non-applied evidence-only scope
- evidence cards for build, UI launch, and screenshot
- authority/safety panel showing source edits, target writes, proposal application, AI authority, hardware authority, and productization remain false
- artifact links with status, digest, and claim scope
- screenshot preview metadata and image path
- open blockers and recommended next action

## Future P8.21 Acceptance Criteria

The next implementation slice should pass only if:

| Criterion | Required result |
|---|---|
| projection JSON emitted | Pass |
| all required input artifacts exist | Pass |
| source refs and target baseline are visible | Pass |
| build/UI/screenshot evidence is visible | Pass |
| screenshot image path is linked | Pass |
| authority false flags are visible | Pass |
| proposal remains non-applied | Pass |
| productization remains blocked | Pass |
| original WindowsUtility target is not mutated | Pass |

## Blocking Conditions

Block projection implementation if it:

- treats the projection as accepted graph authority.
- hides failing or missing evidence.
- omits source edit, target write, AI authority, hardware authority, or productization false flags.
- writes generated artifacts into the WindowsUtility target repo.
- launches the app or captures screenshots without a separate evidence slice.
- claims product readiness.

## Recommendation

Continue to:

```text
P8.21 Shell Workspace Evidence Workbench Projection
```

P8.21 may emit deterministic projection JSON and a static local HTML preview from existing evidence only. It must not mutate the WindowsUtility target or claim productization.
