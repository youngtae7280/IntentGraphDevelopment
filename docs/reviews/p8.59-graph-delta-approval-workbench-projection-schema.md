# P8.59 Graph Delta Approval Workbench Projection Schema

Work item: P8.59 Graph Delta Approval Workbench Projection Schema
Date: 2026-07-10
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: the graph delta approval workbench projection schema was emitted and validated.

## Scope

P8.59 implements the projection layer required before an interactive graph approval workbench can be rendered.

This slice emits deterministic JSON only. It does not implement the HTML graph UI, mutate WindowsUtility, apply proposals, extract packages, launch packaged executables, publish releases, or claim productization readiness.

## Produced Artifacts

- `tools/emit_graph_delta_approval_workbench_projection.py`
- `generated/windowsutility/graph-delta-approval-workbench/p8.59/projection.json`
- `generated/windowsutility/graph-delta-approval-workbench/p8.59/validation-report.json`
- `generated/roadmap/p8.59-graph-delta-approval-workbench-projection-schema-report.json`

## Validation Result

Validation passed.

Counts:

- nodes: `10`
- edges: `7`
- delta steps: `3`
- code diffs: `1`
- graph node diffs: `1`
- graph edge diffs: `1`

The validator checks:

- every edge endpoint exists.
- graph indexes are present.
- changed nodes have graph diff refs.
- changed edges have graph diff refs.
- code nodes have code diff refs.
- code diff affected nodes/edges exist.
- delta steps reference only existing nodes, edges, and diffs.
- false authority and execution/productization flags remain false.

## Covered Diff Examples

Code diff:

- added `tests/RegressionSmoke/Invoke-IntentGraphShellWorkspacePreflight.ps1`

Changed graph node diff:

- `proposal.p8.43-preflight` changed from previewed/not-applied to applied with recorded deviation and target commit.

Changed graph edge diff:

- `edge.proposal-to-code-file` changed from planned-add to applied-add.

## Remaining Work

The next slice must render this projection into a static local HTML prototype with:

- graph canvas
- node/edge selection
- inspector panel
- delta-step highlighting
- code diff panel
- graph element diff panel
- evidence/authority panel
- no network dependency
- no mutation controls

## Recommendation

Continue to:

```text
P8.60 Static Graph Delta Approval Workbench Prototype
```
