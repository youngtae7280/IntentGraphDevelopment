# P8.119 Packaged UI Screenshot Authorization Response Record

Work item: P8.119 Packaged UI Screenshot Authorization Response Record
Date: 2026-07-13
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: exact packaged UI screenshot capture authorization is recorded.

## Recorded Response

The Coordinator provided exactly:

```text
accept sandboxed packaged UI screenshot capture
```

This matches the required response from P8.117/P8.118.

## Authorized Next Verifier Token

The next bounded verifier may use:

```text
accept-sandboxed-packaged-ui-screenshot-capture
```

This token is limited to the next packaged UI screenshot verifier run.

## Narrow Authority Granted

Allowed for the next verifier run only:

- use a temporary sandbox directory
- launch the packaged executable from the sandbox
- observe the packaged UI window geometry
- capture a packaged UI screenshot PNG
- record screenshot metadata and digest in generated output
- terminate the launched process after capture

Still not authorized:

- installer creation
- artifact signing
- credential access
- provider API calls
- release tag creation
- release publishing
- product candidate acceptance
- productization claim

## Boundary Confirmation

This response record did not launch the packaged executable and did not capture a screenshot.

Still false:

- packaged executable launched by this record
- packaged UI screenshot captured by this record
- installer created
- artifact signed
- credential accessed
- provider API called
- release published
- product candidate accepted
- productization claimed

## Recommendation

Continue to `P8.120 Packaged UI Screenshot Capture Verification` using the bounded verifier token above.
