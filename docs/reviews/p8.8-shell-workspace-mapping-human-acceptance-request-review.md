# P8.8 Shell Workspace Mapping Human Acceptance Request

Work item: P8.8 Shell Workspace Mapping Human Acceptance Request
Date: 2026-07-10
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: human acceptance is requested; acceptance is not recorded.

## Scope

P8.8 creates the explicit human acceptance request for the shell-workspace mapping.

This slice is request-only. It does not accept the mapping, modify WindowsUtility, apply proposals, grant AI authority, or authorize productization.

## Acceptance Request

Question:

```text
Accept `unit.windowsutility.shell-workspace` as the first WindowsUtility real-project Intent Unit mapping?
```

Candidate:

```text
unit.windowsutility.shell-workspace
```

Draft mapping:

```text
mapping.windowsutility.shell-workspace.draft
```

Evidence reviewed:

- P8.4 mapping draft
- P8.5 positive verification and negative probes
- P8.6 clean/aligned target state
- P8.7 acceptance readiness review

## What Acceptance Would Mean

If the user accepts, a later slice may create an accepted mapping artifact outside WindowsUtility.

Acceptance would not authorize:

- source edits
- proposal application
- AI code application
- target writes
- productization

## Current State

```text
humanAcceptance.recorded = false
acceptedMappingCreated = false
targetWritesAuthorized = false
proposalApplicationAuthorized = false
aiAuthorityPromoted = false
productizationAuthorized = false
```

## Acceptance Criteria

| Criterion | Result |
|---|---|
| explicit request text created | Pass |
| reviewed evidence listed | Pass |
| acceptance not recorded | Pass |
| mapping not accepted | Pass |
| target writes blocked | Pass |
| proposal application blocked | Pass |
| productization blocked | Pass |

## Required User Decision

The Coordinator/user must answer one of:

- accept shell-workspace mapping
- reject shell-workspace mapping
- revise the mapping draft first

No accepted mapping artifact should be created before that decision.
