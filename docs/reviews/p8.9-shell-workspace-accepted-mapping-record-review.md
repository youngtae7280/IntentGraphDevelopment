# P8.9 Shell Workspace Accepted Mapping Record

Work item: P8.9 Shell Workspace Accepted Mapping Record
Date: 2026-07-10
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: `unit.windowsutility.shell-workspace` is now the first accepted WindowsUtility Intent Unit mapping.

## Scope

P8.9 records the user's explicit `accept` response for the shell-workspace mapping.

This slice creates an accepted mapping artifact and verifier. It does not modify WindowsUtility, apply proposals, grant AI authority, or authorize productization.

## Accepted Mapping

Artifact:

```text
generated/windowsutility/p8.9-shell-workspace-accepted-mapping.json
```

Mapping id:

```text
mapping.windowsutility.shell-workspace.accepted
```

Intent Unit:

```text
unit.windowsutility.shell-workspace
```

Human acceptance:

```text
responseText: accept
recorded: true
accepted: true
```

## Verification

Verifier:

```text
tools/verify_windowsutility_accepted_mapping.py
```

Verification report:

```text
generated/windowsutility/p8.9-shell-workspace-accepted-mapping-verification-report.json
```

## Still Not Authorized

- target writes
- source edits
- proposal application
- AI code application
- productization

## Acceptance Criteria

| Criterion | Result |
|---|---|
| user acceptance recorded | Pass |
| accepted mapping artifact created | Pass |
| selected source refs verified | Pass |
| target remains clean/aligned | Pass |
| target writes remain blocked | Pass |
| proposal application remains blocked | Pass |
| productization remains blocked | Pass |

## Recommendation

Continue to:

```text
P8.10 Shell Workspace Accepted Mapping Negative Probes
```

P8.10 should prove that stale accepted mapping refs, missing human acceptance, and unsafe authority promotion fail deterministically.
