# P1.3 Code-First Maintenance Delta Review

Work item: P1.3 Tiny Code-First Maintenance Delta Probe
Date: 2026-07-09
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: continue

## Scope

P1.3 adds the first code-first maintenance delta probe. CF0 starts from a hand-written Python calculator, captures before-state facts, applies a tiny accepted `mul` behavior change, extracts after-state facts, updates overlay mappings, and verifies the delta without source text equality.

The verified loop is:

```text
C0 -> C1
X0 -> X1
I0/M0 -> I1/M1
E/A/H updated
Verify preservation + new behavior -> pass
```

## Produced Artifacts

- updated `docs/examples/cf0-python-cli-calculator/source/calc.py`
- updated `docs/examples/cf0-python-cli-calculator/intentgraph.overlay.json`
- `docs/examples/cf0-python-cli-calculator/deltas/p1.3-add-mul.delta.json`
- `generated/cf0-python-cli-calculator/p1.3-before-code-facts.json`
- `generated/cf0-python-cli-calculator/p1.3-before-overlay.json`
- `generated/cf0-python-cli-calculator/p1.3-after-code-facts.json`
- `generated/cf0-python-cli-calculator/p1.3-maintenance-delta-report.json`
- `tools/verify_code_first_delta.py`

## Acceptance Criteria

| Criterion | Result |
|---|---|
| before-state facts captured before mutation | Pass |
| before overlay digest and mapping obligation count verified | Pass |
| hand-written source changed for accepted delta | Pass |
| add/sub behavior preserved | Pass |
| mul behavior added and passing | Pass |
| new `unit.behavior.mul` exists | Pass |
| new mul codeRefs/codeFactRefs/mappingObligations resolve | Pass |
| existing add/sub mappings remain resolved | Pass |
| delta evidence/authority/history ids exist and are verified | Pass |
| source text equality is not required | Pass |
| hidden generated-code snapshot is not used | Pass |
| B0 generated-code regression remains passing | Pass |

## Report Evidence

The delta report records:

- `mode: code-first-maintenance-delta`
- `result: pass`
- before and after source digests normalized as `sha256:<hex>`
- before and after code facts digests and counts
- before and after overlay digests
- before and after mapping obligation counts
- added code facts including `fact.function.mul`
- behavior verification for add, sub, and mul
- mapping verification result `pass`
- delta-declared evidence, authority, and history ids verified against the after overlay

`generated/cf0-python-cli-calculator/p1.3-before-overlay.json` is a baseline artifact recovered from parent commit `f76246f634d6c61ca0b9e7e328ba2fb667b61115` for verifier reproducibility. It is not presented as newly captured before mutation; the before facts and digest values were captured before the P1.3 source/overlay mutation.

## P2 Decisions

- `P1.3-P2-001`: the delta is hand-authored, not planned by a general patch planner. Decision: defer because this phase tests verifiable maintenance evidence, not planning.
- `P1.3-P2-002`: the extractor remains tiny and Python-specific. Decision: defer broad extraction and external code intelligence integration.
- `P1.3-P2-003`: behavior verification is smoke-test based, not semantic equivalence. Decision: defer full behavior equivalence.

## Recommendation

Continue. The next slice may either reduce B0 generated-code snapshot dependence or add a second code-first maintenance probe that exercises removal/renaming/conflict handling.
