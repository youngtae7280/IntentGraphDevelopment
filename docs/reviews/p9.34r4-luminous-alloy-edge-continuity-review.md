# P9.34.R4 Luminous Alloy and Edge Continuity Review

## Decision

The visibility and deep-zoom continuity correction passes deterministic, browser,
negative-probe, and loopback server gates. The final WindowsUtility screenshot was
also inspected after the numeric gate passed; the first darker revision was rejected
and brightened before this decision.

## Verified Result

- all 8,194 nodes and 7,986 relations remain loaded;
- overview material evidence records 453 opaque samples, 379 chromatic samples, and
  mean opaque luminance `56.706`;
- ordinary code facts now retain a visible community-colored body in the full graph;
- active material identity is `cached-luminous-nebula-alloy-v9`;
- the browser gate selects an ordinarily dashed `invokes-syntax` relation and renders
  it as a solid `0.65px` inspection line at `0.30` opacity and `512x`;
- the sampled selected endpoint has `12x14px` opaque bounds and 53 color buckets;
- ordinary low-detail code visibility and selected-edge canvas continuity are checked
  independently of aggregate material pixels;
- ordinary code fact evidence records `0.82` element opacity, `0.30` background
  opacity, and `2.432px` rendered width in the full graph;
- edge-to-visible-node diameter difference is `1.296px`, and all 13 edge corridor
  samples are observed with no missing run;
- deep zoom, pan, zoom-out recovery, selection inspection, viewport-local material
  rendering, and the 96-entry sprite cache remain intact;
- graph facts, relations, deterministic layout coordinates, source, workflow,
  evidence, authority, history, graph delta, and code diff are unchanged.

## Evidence

- `generated/roadmap/p9.34r4-luminous-alloy-browser-report.json`
- `generated/roadmap/p9.34r4-luminous-alloy-browser-screenshot.png`
- `generated/roadmap/p9.34r4-luminous-alloy-browser-negative-probes-report.json`
- `generated/roadmap/p9.34r4-luminous-alloy-server-smoke-report.json`

The final gate consists of 33 in-page runtime checks, 37 fail-closed browser/output
mutations, and 30 loopback server/projection checks.

## Boundary

This is a renderer-only correction. Pixel thresholds guard repeatable regressions,
but they do not make visual taste a machine authority. The screenshot remains human
review evidence, not a graph or workflow mutation.
