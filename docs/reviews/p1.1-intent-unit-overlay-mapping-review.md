# P1.1 Intent Unit Overlay Mapping Review

Work item: P1.1 Intent Unit Overlay Mapping Revision
Date: 2026-07-09
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: continue

## Scope

P1.1 resolves the naming ambiguity left after P1.R. The historical `P1.0 Intent Unit Grammar Revision` remains a generated-code experiment review. The current active overlay revision is `P1.1 Intent Unit Overlay Mapping Revision`.

This review covers:

- B0 Intent Units as semantic overlay mapping units
- `codeRefs`, `codeFactRefs`, and `mappingObligations`
- generated-code projection/reconstruction fields scoped to generated-code mode
- tool validation and report visibility for unit overlay mappings
- preservation of evidence, authority, semantic history, AI proposal, and workbench boundaries

## Produced Artifacts

- revised `docs/examples/b0-python-cli-calculator.graph.json`
- updated GraphIR and validation docs
- updated generated-code, retrofit, verifier, and workbench contracts
- updated `tools/native_compile.py`
- updated `tools/retrofit_reconstruct.py`
- updated `tools/verify_roundtrip.py`
- updated `tools/emit_workbench_projection.py`
- regenerated B0 generated artifacts and reports

## Review Findings

### P0/P1

No unresolved P0 or P1 issues remain.

### P2

- `P1.1-P2-001`: exact generated-code round-trip still uses `hiddenState.sourceGraphSnapshot`. Decision: defer with explicit visibility. P1.1 adds unit-level mapping digests and preservation reports, but it does not remove the snapshot dependency.
- `P1.1-P2-002`: code facts are statically declared or generated-preservation-backed, not extracted by an external analyzer. Decision: defer because external extraction is a non-goal for this phase.

### P3

- Add executable tests around negative mapping-obligation probes in a later harness slice.

## Acceptance Criteria

| Criterion | Result |
|---|---|
| P1.0/P1.1 naming ambiguity resolved | Pass |
| B0 Intent Units include `codeRefs` | Pass |
| B0 Intent Units include `codeFactRefs` | Pass |
| B0 Intent Units include `mappingObligations` | Pass |
| generated-code fields are scoped to generated-code mode | Pass |
| code nodes/refs remain references or facts, not code text | Pass |
| native compile validates overlay mapping fields | Pass |
| retrofit validates unit mapping metadata digests | Pass |
| verifier reports mapping obligation preservation | Pass |
| workbench projection exposes unit overlay mappings | Pass |
| AI proposal remains non-authoritative | Pass |

## Residual Risk

P1.1 proves the B0 fixture can carry overlay mapping fields through the existing metadata-backed generated-code experiment. It does not prove code-first maintenance over a hand-written codebase, source edits, external code fact extraction, or behavior equivalence checking.

## Recommendation

Continue, but do not broaden to a larger benchmark yet. The next slice should either reduce snapshot dependence or introduce a tiny code-first maintenance fixture with declared code facts and behavior-preservation expectations.
