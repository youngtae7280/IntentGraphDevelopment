# Generated by IntentGraph compiler contract: native-python-b0-overlay-unit-v0
# Source graph: ig.bench.b0.python-cli-calculator
# Graph digest: sha256:a43a99d2cb2c4bb02c0822db74d4dbb00352bb9e9bd52d125b64cf23cafe870a
"""Generated CLI calculator for the IntentGraph B0 benchmark."""

from __future__ import annotations

import sys


def add(left: int, right: int) -> int:
    return left + right


def sub(left: int, right: int) -> int:
    return left - right


def main(argv: list[str] | None = None) -> int:
    if argv is None:
        argv = sys.argv[1:]
    if len(argv) != 3:
        print("usage: calc {add|sub} LEFT RIGHT", file=sys.stderr)
        return 2
    operation, left_raw, right_raw = argv
    if operation not in {"add", "sub"}:
        print(f"unsupported operation: {operation}", file=sys.stderr)
        return 2
    try:
        left = int(left_raw)
        right = int(right_raw)
    except ValueError:
        print("LEFT and RIGHT must be integers", file=sys.stderr)
        return 2
    if operation == "add":
        result = add(left, right)
    else:
        result = sub(left, right)
    print(result)
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
