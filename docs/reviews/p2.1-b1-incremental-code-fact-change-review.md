# P2.1 B1 Incremental Code Fact Change Probe Review

Work item: P2.1 B1 Incremental Code Fact Change Probe
Date: 2026-07-10
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: pass; continue to B1 Intent Unit mapping only after Phase B fact boundaries remain stable.

## Scope

P2.1 proves that a bounded one-file change in B1 produces a deterministic and reviewable code fact delta.

This slice does not add Intent Unit mapping, AI planning, workbench UI, broad extraction, TypeScript compiler integration, or productization.

## Implemented Change

Changed exactly one file:

```text
docs/examples/b1-typescript-rest-api/source/src/service/todoService.ts
```

Added function:

```text
completeTodo(todo: Todo): Todo
```

Before artifacts:

- `generated/b1-typescript-rest-api/p2.1-before-code-facts.json`
- `generated/b1-typescript-rest-api/p2.1-before-source/src/service/todoService.ts`

After artifacts:

- `generated/b1-typescript-rest-api/p2.1-after-code-facts.json`
- `generated/b1-typescript-rest-api/code-facts.json`
- `generated/b1-typescript-rest-api/code-facts-validation-report.json`

Delta and reports:

- `docs/examples/b1-typescript-rest-api/deltas/p2.1-add-complete-todo.delta.json`
- `tools/verify_b1_incremental_change.py`
- `tools/run_b1_incremental_negative_probes.py`
- `generated/b1-typescript-rest-api/p2.1-incremental-change-report.json`
- `generated/b1-typescript-rest-api/p2.1-incremental-negative-probes-report.json`

## Observed Delta

Added facts:

- `fact.function.completetodo`

Changed facts:

- `fact.call.addtodo.createtodo`
- `fact.call.addtodo.requiretitle`
- `fact.file.src_service_todoservice_ts`
- `fact.function.addtodo`
- `fact.function.listtodos`
- `fact.import.src_service_todoservice_ts.createtodo`
- `fact.import.src_service_todoservice_ts.requiretitle`
- `fact.import.src_service_todoservice_ts.todo`
- `fact.module.src_service_todoservice`

Added relations:

- `rel.contains.src_service_todoservice_ts.fact_function_completetodo`

Removed facts and relations:

- none

Unchanged source files retained identical digests and file-local facts.

## Negative Probe Coverage

P2.1 adds repeatable negative probes for:

- wrong changed file declaration
- missing added fact expectation
- missing changed fact expectation
- unexpected removed fact expectation
- missing added relation expectation
- forbidden Intent mapping claim
- forbidden workbench claim

Every probe must fail for the expected reason.

## Acceptance Criteria

| Criterion | Result |
|---|---|
| before-state facts captured before mutation | Pass |
| exactly one B1 source file changed | Pass |
| after facts validate under B1 schema | Pass |
| expected fact delta matches actual fact delta | Pass |
| expected relation delta matches actual relation delta | Pass |
| unchanged file digests and file-local facts remain stable | Pass |
| negative probes are repeatable | Pass |
| broad extractor/Intent mapping/UI/AI claims remain false | Pass |

## Open Issues

| ID | Severity | Summary | Decision |
|---|---|---|---|
| P2.1-P1-001 | P1 | B1 facts still are not mapped to Intent Units. | Phase C should not open until Phase B fact boundaries are reviewed; next slice can prepare B1 overlay mapping plan. |
| P2.1-P2-001 | P2 | Incremental comparison is report-based, not a performance benchmark. | Accept for P2.1; performance measurement should come before real-project adoption. |
| P2.1-P2-002 | P2 | Extractor remains fixture-bounded. | Accept; this is the intended anti-broad-extractor boundary. |

## Recommendation

Continue to:

```text
P2.2 B1 Code Fact Boundary Review and Phase C Entry Plan
```

P2.2 should review Phase B static and incremental fact evidence, decide whether B1 code facts are stable enough to begin Intent mapping, and define the first Phase C mapping slice. It should be plan/review-oriented unless the Coordinator explicitly requests another Phase B hardening slice.
