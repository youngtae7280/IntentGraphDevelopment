# P8.35 Static Local Workbench Export Reviewer Orientation Revision

Work item: P8.35 Static Local Workbench Export Reviewer Orientation Revision
Date: 2026-07-10
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: pass; a revised static export now explains what it is, what it represents, what to inspect, and what it is not.

## Scope

P8.35 responds to the P8.34 user review result:

```text
revise: 일단 이게 뭔지 잘 모르겠어. 뭘 나타내는거야? 내가 뭘 봐야해?
```

This slice updates only generated IntentGraphDevelopment static export tooling and generated product-surface artifacts. It does not mutate WindowsUtility, apply proposals, collect new WindowsUtility evidence, launch WindowsUtility, capture new WindowsUtility screenshots, grant AI authority, grant hardware authority, package a product, release anything, or claim productization readiness.

## Revised Export

- `generated/product-surfaces/windowsutility-shell-workspace-static-workbench/p8.35/index.html`

The revised first screen now includes:

- `Before reading the records`
- `What This Represents`
- `What To Look At`
- `What this is not`

## What The Page Is

The page is a static review dashboard for one proposed IntentGraph workflow over WindowsUtility shell/workspace code.

It is not the WindowsUtility product itself. It is a local review surface that lets a human inspect whether IntentGraph has correctly gathered the mapping, evidence, and safety boundaries for one small WindowsUtility workflow before any source changes are allowed.

## What The Page Represents

- accepted mapping
- covered code surface
- non-applied smoke-evidence proposal
- build, launch, screenshot, and browser validation evidence
- false authority flags and blocked productization gates

## What The Reviewer Should Inspect

The reviewer should answer whether:

1. the WindowsUtility area is understandable.
2. the evidence exists and its source is understandable.
3. the proposal is clearly not applied to source code.
4. blocked authority and productization gates are visible.
5. the screen is clear enough to guide the next IntentGraph iteration.

## Validation

Passed:

- static export validation
- static export negative probes
- headless Chromium browser validation
- P8.31 default validation compatibility check
- Python compile for changed static export tools
- placeholder/mojibake scan
- whitespace checks

Browser validation confirmed:

- start/orientation section visible
- what-this-represents section visible
- what-to-look-at checklist visible
- not-WindowsUtility-app warning visible
- local review surface wording visible
- screenshot rendered
- selection records update details

## Boundary Confirmation

Still unauthorized:

- source edits
- target writes
- proposal application
- new sandbox runs
- new UI launches
- new screenshots
- AI authority promotion
- hardware actions
- packaging
- release
- productization

## Recommendation

Continue to:

```text
P8.36 Static Local Workbench Export Orientation Review Request
```

The user/coordinator should review the revised p8.35 export, not the earlier p8.31 export.
