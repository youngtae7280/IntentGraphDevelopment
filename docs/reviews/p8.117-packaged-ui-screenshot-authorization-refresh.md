# P8.117 Packaged UI Screenshot Authorization Request Refresh

Work item: P8.117 Packaged UI Screenshot Authorization Request Refresh
Date: 2026-07-13
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: screenshot capture authorization is requested, not recorded.

## Why This Request Is Now Actionable

P8.107 completed package extraction inventory verification and P8.114 completed packaged executable launch smoke. Those satisfy the preconditions that blocked the original P8.79 screenshot request.

## Required Accepted Response

To authorize the next bounded packaged UI screenshot verifier run, the Coordinator must provide exactly:

```text
accept sandboxed packaged UI screenshot capture
```

Allowed responses:

- accept sandboxed packaged UI screenshot capture
- reject packaged UI screenshot capture
- revise screenshot scope first

The current-goal ordinary `accept/revise/blocked` shortcut does not authorize screenshot capture. Screenshot capture is a separate productization evidence authority.

## Boundary Confirmation

This request did not launch the packaged executable and did not capture screenshots.

Still false:

- packaged executable launched by this request
- packaged UI screenshot captured
- installer created
- artifact signed
- credential accessed
- provider API called
- release published
- product candidate accepted
- productization claimed

## Recommendation

Wait for the exact accepted response before any packaged UI screenshot capture run.
