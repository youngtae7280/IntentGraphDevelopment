# P1.14 Usage Arity Overlay Contract Review

Work item: P1.14 Tiny Code-First Overlay-Only Usage Arity Contract Delta Probe
Date: 2026-07-10
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: continue

## Scope

P1.14 models CF0's existing wrong-arity usage behavior as an accepted IntentGraph overlay contract. The source already prints `usage: calc {add|sub|mul} LEFT RIGHT` to stderr and exits 2 when the argument count is wrong. This slice adds semantic overlay coverage, not product behavior.

This is an overlay-only contract delta. It does not change CF0 source bytes, add broad control-flow inference, build UI, alter B0 generated-code behavior, introduce dependencies, or add a P1.14 negative harness.

## Produced Artifacts

- `generated/cf0-python-cli-calculator/p1.11-after-source/calc.py`
- `generated/cf0-python-cli-calculator/p1.11-after-code-facts.json`
- `generated/cf0-python-cli-calculator/p1.11-after-overlay.json`
- `generated/cf0-python-cli-calculator/p1.14-before-source/calc.py`
- `generated/cf0-python-cli-calculator/p1.14-before-code-facts.json`
- `generated/cf0-python-cli-calculator/p1.14-before-overlay.json`
- `docs/examples/cf0-python-cli-calculator/deltas/p1.14-overlay-usage-arity.delta.json`
- updated `docs/examples/cf0-python-cli-calculator/intentgraph.overlay.json`
- `generated/cf0-python-cli-calculator/p1.14-overlay-usage-arity-delta-report.json`
- updated `generated/cf0-python-cli-calculator/cf0-historical-state-index.json`
- updated P1.12 negative harness baseline
- updated roadmap, phase-entry, and validation rules

## Acceptance Criteria

| Criterion | Result |
|---|---|
| P1.11 after-state source/facts/overlay captured before current overlay change | Pass |
| P1.12 harness uses historical P1.11 after-state artifacts | Pass |
| P1.14 source bytes remain unchanged | Pass |
| usage/arity code fact exists | Pass |
| `unit.behavior.usage-arity` added | Pass |
| usage/arity mapping obligations resolve | Pass |
| usage/arity behavior smoke exits 2 with expected stderr | Pass |
| P1.14 evidence record exists | Pass |
| P1.14 authority record exists | Pass |
| P1.14 history record exists | Pass |
| P1.4 historical negative harness still passes | Pass |
| P1.10 historical negative harness still passes | Pass |
| P1.12 historical negative harness still passes | Pass |
| P1.7/P1.9/P1.11 historical reports still pass | Pass |
| state index marks P1.14 current and P1.11 historical | Pass |

## Contract Coverage

P1.14 uses existing deterministic code fact `fact.function.main.stderr.usage_calc_add_sub_mul_left_right` and existing return fact `fact.function.main.return.2`. No extractor expansion was required.

The new verification case is `test.case.usage-arity`, which runs `python calc.py add 2` and expects:

- exit code `2`
- empty stdout
- stderr `usage: calc {add|sub|mul} LEFT RIGHT\n`

## Historical Boundary

P1.12 is now historical. Its harness uses:

- `generated/cf0-python-cli-calculator/p1.11-after-code-facts.json`
- `generated/cf0-python-cli-calculator/p1.11-after-overlay.json`
- `generated/cf0-python-cli-calculator/p1.11-after-source/calc.py`

The P1.14 current state uses current CF0 facts and overlay. The state index records P1.11 as `cf0.state.p1.11.after-overlay-invalid-integer` and P1.14 as `cf0.state.p1.14.current-overlay-usage-arity`.

## P2 Decisions

- `P1.14-P2-001`: no P1.14-specific negative harness was added. Decision: defer to a follow-up hardening slice, mirroring P1.10 and P1.12.
- `P1.14-P2-002`: the extractor still records stderr facts by literal text and does not infer full branch semantics. Decision: acceptable for CF0 because the behavior is verified by smoke checks and mapped through explicit overlay obligations.

## Recommendation

Continue. A good next slice is a repeatable P1.14 usage/arity negative harness before adding more semantic coverage.
