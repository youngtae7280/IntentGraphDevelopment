# P8.15 Shell Workspace Sandboxed Smoke Evidence Dry Run Review

Work item: P8.15 Shell Workspace Sandboxed Smoke Evidence Dry Run
Date: 2026-07-10
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: pass; sandboxed build-only smoke evidence completed without modifying the original WindowsUtility repo.

## Scope

P8.15 performs the first real WindowsUtility shell/workspace evidence dry run from a disposable sandbox copy.

The dry run copies WindowsUtility to a sibling sandbox, runs `dotnet build WindowsUtility.sln --nologo` in the sandbox, records the build log, removes the sandbox, and proves the original target repo remains clean/aligned.

It does not launch the WPF UI, collect screenshots, invoke hardware, edit source, apply proposals, grant AI authority, or claim productization readiness.

## Implemented Artifacts

- `tools/run_windowsutility_sandboxed_smoke_evidence.py`
- `generated/windowsutility/p8.15-sandboxed-smoke-evidence-report.json`
- `generated/windowsutility/p8.15-sandboxed-smoke-evidence-build.log`
- `generated/roadmap/p8.15-shell-workspace-sandboxed-smoke-evidence-dry-run-report.json`

## Evidence Summary

| Evidence | Result |
|---|---|
| accepted shell/workspace source refs resolved in sandbox | Pass |
| sandbox build command completed | Pass |
| build exit code | 0 |
| build warnings | 0 |
| build errors | 0 |
| original target status before run | `## main...origin/main` |
| original target status after run | `## main...origin/main` |
| sandbox removed after run | Pass |

## Important Correction

The first attempt placed the sandbox under `_intentgraph_sandboxes`, which broke the WindowsUtility relative reference to sibling `Utility_Windows\Release_x64\SmartComm2.dll`.

The final accepted dry run places the sandbox directly under `C:\Users\ytkim\Desktop\kyt_work`, preserving the same sibling relationship used by the original repo while still avoiding writes to the original WindowsUtility repo.

## Open Issues

| ID | Severity | Summary | Decision |
|---|---|---|---|
| P8.15-P1-001 | P1 | UI launch and screenshot evidence are still absent. | Defer to a later bounded UI evidence slice. |
| P8.15-P2-001 | P2 | The dry run uses a fixed local sibling path for sandbox compatibility. | Accept for WindowsUtility; revisit if generalized. |
| P8.15-P2-002 | P2 | Build performs restore in the sandbox and may use machine-level NuGet cache. | Accept; original target repo remains unchanged. |

## Recommendation

Continue to:

```text
P8.16 Shell Workspace UI Evidence Boundary Plan
```

P8.16 should decide whether UI launch/screenshot evidence is safe and how to capture it without hardware actions or original target writes.
