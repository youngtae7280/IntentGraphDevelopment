# P1.5 B0 Typed Preservation Review

Work item: P1.5 B0 Typed Preservation Metadata Snapshot Reduction
Date: 2026-07-09
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: continue

## Scope

P1.5 reduces B0 generated-code snapshot dependence by adding typed preservation metadata for selected non-code graph domains. The full hidden source graph snapshot remains present for exact full-graph equality; this slice proves that important domains are no longer opaque inside that snapshot alone.

This is a generated-code metadata slice only. It does not broaden B0 behavior, add a new benchmark, claim code-only reconstruction, or remove `hiddenState.sourceGraphSnapshot`.

## Produced Artifacts

- updated `tools/native_compile.py`
- updated `tools/retrofit_reconstruct.py`
- updated `tools/verify_roundtrip.py`
- updated `tools/emit_workbench_projection.py`
- regenerated `generated/b0-python-cli-calculator/calc.intentgraph.json`
- regenerated `generated/b0-python-cli-calculator/native-diagnostics.json`
- regenerated `generated/b0-python-cli-calculator/retrofit-diagnostics.json`
- regenerated `generated/b0-python-cli-calculator/reconstructed.graph.json`
- regenerated `generated/b0-python-cli-calculator/roundtrip-report.json`
- regenerated `generated/b0-python-cli-calculator/workbench-projection.json`

## Typed Domains

`typedPreservation.version` is `p1.5-typed-preservation-v0`.

The typed preservation domains are:

- `intentUnits`
- `unitEdges`
- `evidence`
- `authority`
- `history`

Each domain has sorted records, a deterministic count, and a deterministic `sha256:` digest. Retrofit validates each typed domain against the corresponding domain in `hiddenState.sourceGraphSnapshot` before accepting reconstruction.

## Acceptance Criteria

| Criterion | Result |
|---|---|
| B0 native compile passes | Pass |
| B0 retrofit reconstruction passes | Pass |
| B0 round-trip verification passes | Pass |
| proposal validation and workbench projection pass | Pass |
| metadata contains typed preservation records for selected domains | Pass |
| retrofit diagnostics report typed preservation validation | Pass |
| round-trip report exposes typed preservation status | Pass |
| stale typed digest negative probe fails | Pass |
| missing typed evidence record negative probe fails | Pass |
| full snapshot remains present and acknowledged | Pass |

## Report Evidence

Generated reports show:

- `calc.intentgraph.json` includes `typedPreservation`
- `retrofit-diagnostics.json` includes `typedPreservation.result: pass`
- `roundtrip-report.json` includes `preservation.typedPreservation.result: pass`
- `workbench-projection.json` includes `roundTrip.typedPreservation`
- `m5ClaimScope.hiddenStateSnapshotUsed: true`
- `m5ClaimScope.typedPreservationUsed: true`

## Negative Probes

Temporary command-level probes were run:

- corrupt `typedPreservation.domains.evidence.digest` to `sha256:0000`; retrofit failed with `typedPreservation evidence digest does not match records`
- remove one typed evidence record from a temporary metadata copy; retrofit failed with `typedPreservation evidence digest does not match records`

## P2 Decisions

- `P1.5-P2-001`: `hiddenState.sourceGraphSnapshot` remains present. Decision: defer removal because exact full graph equality still depends on metadata-backed reconstruction beyond the selected typed domains.
- `P1.5-P2-002`: typed records cover selected domains only. Decision: defer additional typed domains until a later slice can validate them without over-expanding this milestone.
- `P1.5-P2-003`: negative probes are temporary command-level checks. Decision: defer a B0 typed-preservation negative harness until the next hardening slice if the Coordinator chooses that path.

## Recommendation

Continue. A good next slice is either a B0 typed-preservation negative harness, mirroring P1.4 for generated-code metadata, or a second small typed-domain expansion that reduces full snapshot dependence further without claiming code-only reconstruction.
