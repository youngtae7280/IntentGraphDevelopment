# P8.3 WindowsUtility Accepted Mapping Candidate Selection

Work item: P8.3 WindowsUtility Accepted Mapping Candidate Selection
Date: 2026-07-10
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: select `unit.windowsutility.shell-workspace` as the first accepted-mapping candidate; do not accept it yet.

## Scope

P8.3 selects the first WindowsUtility mapping hypothesis to prepare for future accepted mapping work.

This slice is report-only. It does not accept the mapping, modify WindowsUtility, inspect untracked `.devview/`, apply proposals, grant AI authority, or authorize productization.

## Candidate Comparison

| Candidate | Surface size | Main risk | Selection result |
|---|---|---|---|
| `unit.windowsutility.shell-workspace` | 2 project refs | UI/shell smoke evidence | Selected |
| `unit.windowsutility.core-services` | 3 project refs | native boundary risk | Deferred |
| `unit.windowsutility.utility-modules` | 11 project refs | broad module/hardware scope | Deferred |
| `unit.windowsutility.tests-and-smoke` | 4 project refs | hardware fallback evidence | Deferred |

## Selection Rationale

`unit.windowsutility.shell-workspace` is the smallest candidate surface and is likely the best first accepted mapping target because it can be validated with shell/workspace navigation evidence before hardware-aware module boundaries are touched.

It is still only a candidate. Acceptance is blocked until:

- repository baseline is resolved or explicitly scoped.
- source refs are revalidated against a baseline inventory.
- UI/shell evidence requirements are declared.
- human authority requirements are declared.
- stale/missing mapping negative probes are added.

## Selected Candidate

```text
unit.windowsutility.shell-workspace
```

Candidate refs:

- `src/WindowsUtility.App/WindowsUtility.App.csproj`
- `src/WindowsUtility.Shell/WindowsUtility.Shell.csproj`

Required future evidence:

- shell navigation smoke test
- screenshot or UI surface evidence
- source digest baseline for selected refs

Required future authority:

- user acceptance for shell/workspace mapping
- coordinator approval before any target write

## Acceptance Criteria

| Criterion | Result |
|---|---|
| one candidate selected | Pass |
| mapping remains unaccepted | Pass |
| target writes remain blocked | Pass |
| evidence requirements identified | Pass |
| authority requirements identified | Pass |
| productization remains blocked | Pass |

## Recommendation

The next safe work is:

```text
P8.4 WindowsUtility Shell Workspace Accepted Mapping Draft
```

P8.4 may prepare an accepted-mapping draft outside WindowsUtility only if it records that the target baseline remains unresolved. It must not accept the draft, mutate source, or apply proposals without explicit Coordinator approval.
