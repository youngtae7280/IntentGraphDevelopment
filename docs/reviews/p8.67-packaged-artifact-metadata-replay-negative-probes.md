# P8.67 Packaged Artifact Metadata Replay Negative Probes

Work item: P8.67 Packaged Artifact Metadata Replay Negative Probes
Date: 2026-07-10
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: metadata replay negative probes passed.

## Scope

P8.67 verifies that the P8.66 metadata replay tool rejects unsafe or stale package metadata inputs.

The harness mutates temporary copies only. It does not extract the committed package, run the packaged executable, launch UI, create installers, sign artifacts, read credentials, call provider APIs, publish releases, or claim productization readiness.

## Result

Report:

```text
generated/windowsutility/package-artifact/p8.67/negative-probes-report.json
```

Result:

```text
pass
```

Probe count: `8`

All expected failures were observed.

## Covered Failures

- manifest sha256 mismatch
- manifest byte length mismatch
- manifest file count mismatch
- boundary extraction authority drift
- boundary packaged execution authority drift
- package byte mutation
- non-zip package
- missing required package entry

## Boundary Confirmation

Still false:

- package extraction performed
- packaged executable launched
- packaged UI launched
- installer created
- artifact signed
- credential accessed
- provider API called
- release published
- productization claimed

## Recommendation

Continue to:

```text
P8.68 Packaged Artifact Verification Authorization Request
```
