# P9.34.R2 Stellar Vitreous Rendering Review

## Decision

The live-review correction passes its deterministic, browser, and server gates and is
ready for user visual review.

## Verified Result

- actual camera, logical zoom, and effective geometry zoom reach `256`;
- direct maximum zoom remains centered on a real graph element;
- a hidden selected element is rejected as an anchor and falls back to a visible node;
- maximum-zoom selected relation width is `0.18px` at `0.34` opacity;
- active material identity is `cached-stellar-vitreous-v5`;
- selected material pixels exceed opacity, chroma, color-bucket, and luminance bounds;
- the cache is bounded to 96 sprites and deep rendering remains viewport-local;
- the external browser validator independently checks the unselected deep-zoom anchor,
  sprite-cache bound, and settled interaction durations;
- all 8,194 nodes and 7,986 relations remain loaded;
- graph facts, graph relations, layout coordinates, source, workflow, evidence,
authority, and history are unchanged.

The final gate consists of 27 in-page runtime checks, 28 fail-closed browser
observation mutations, and 30 loopback server/projection checks.

## Evidence

- `generated/roadmap/p9.34r2-stellar-vitreous-browser-report.json`
- `generated/roadmap/p9.34r2-stellar-vitreous-screenshot.png`
- `generated/roadmap/p9.34r2-stellar-vitreous-negative-probes-report.json`
- `generated/roadmap/p9.34r2-stellar-vitreous-server-smoke-report.json`

## Residual Risk

Pixel checks prove that the material exists, is detailed, and stays bounded. The
45-second external wall-clock gate bounds the whole capture; virtual-time interaction
durations are diagnostic rather than individual wall-clock claims. A person's
premium-material judgment remains a visual-review decision.
