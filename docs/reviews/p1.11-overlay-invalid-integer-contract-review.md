# P1.11 Overlay-Only Invalid Integer Contract Review

Work item: P1.11 Tiny Code-First Overlay-Only Input Validation Contract Delta Probe
Date: 2026-07-10
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: continue

## Scope

P1.11 models CF0's existing invalid-integer input behavior as an explicit IntentGraph overlay contract. The source already catches `ValueError`, writes `LEFT and RIGHT must be integers` to stderr, and exits `2`. P1.11 increases overlay coverage without changing source bytes.

This is an overlay-only contract coverage slice. It does not add product behavior, add broad control-flow inference, build UI, alter B0 generated-code behavior, introduce dependencies, or add a P1.11 negative harness.

## Produced Artifacts

- `generated/cf0-python-cli-calculator/p1.9-after-source/calc.py`
- `generated/cf0-python-cli-calculator/p1.9-after-code-facts.json`
- `generated/cf0-python-cli-calculator/p1.9-after-overlay.json`
- `generated/cf0-python-cli-calculator/p1.7-after-source/calc.py`
- `generated/cf0-python-cli-calculator/p1.7-after-code-facts.json`
- `generated/cf0-python-cli-calculator/p1.7-after-overlay.json`
- `generated/cf0-python-cli-calculator/p1.11-before-source/calc.py`
- `generated/cf0-python-cli-calculator/p1.11-before-code-facts.json`
- `generated/cf0-python-cli-calculator/p1.11-before-overlay.json`
- `docs/examples/cf0-python-cli-calculator/deltas/p1.11-overlay-invalid-integer.delta.json`
- `generated/cf0-python-cli-calculator/p1.11-overlay-invalid-integer-delta-report.json`
- updated `docs/examples/cf0-python-cli-calculator/intentgraph.overlay.json`
- updated `tools/run_cf0_overlay_contract_negative_probes.py`
- updated `tools/emit_cf0_historical_state_index.py`

## Acceptance Criteria

| Criterion | Result |
|---|---|
| P1.10 harness uses historical P1.9 after-state artifacts | Pass |
| CF0 source bytes unchanged | Pass |
| invalid-integer code fact exists | Pass |
| `unit.behavior.invalid-integer-input` exists | Pass |
| mapping obligations resolve to extracted facts | Pass |
| invalid-integer smoke check verifies stderr and exit code | Pass |
| P1.11 evidence id resolves | Pass |
| P1.11 authority id resolves | Pass |
| P1.11 history id resolves | Pass |
| existing add/sub/mul/unsupported units remain preserved | Pass |
| source text equality is not required | Pass |
| hidden generated-code snapshot is not used | Pass |
| P1.4 historical harness still passes | Pass |
| P1.7 historical report still passes | Pass |
| P1.9 historical report still passes | Pass |
| P1.10 negative harness still passes | Pass |
| state index marks P1.11 as current | Pass |

## Contract Coverage

P1.11 uses the existing deterministic code fact:

- `fact.function.main.stderr.left_and_right_must_be_integers`

The overlay adds:

- `unit.behavior.invalid-integer-input`
- `mapping.invalid-integer.behavior-to-code`
- `test.case.invalid-integer`
- `evidence.p1.11-overlay-invalid-integer-delta`
- `authority.record.p1.11-human`
- `history.delta.p1.11-overlay-invalid-integer`

## State Boundary

P1.10 now uses historical P1.9 after-state artifacts. The current state index records:

- `cf0.state.p1.9.after-overlay-unsupported-operation` as historical
- `cf0.state.p1.11.current-overlay-invalid-integer` as current
- `cf0.transition.p1.11.overlay-invalid-integer`

## P2 Decisions

- `P1.11-P2-001`: no P1.11-specific negative harness is added. Decision: defer to a follow-up hardening slice, mirroring P1.10, unless the Coordinator chooses broader cleanup first.
- `P1.11-P2-002`: the extractor still records simple stderr-output facts rather than full exception/control-flow semantics. Decision: defer richer control-flow extraction until another contract requires it.

## Recommendation

Continue. A good next slice is a P1.11 negative harness, or a small cleanup that factors repeated CF0 historical-baseline harness patterns if the Coordinator wants to reduce duplication before further coverage.
