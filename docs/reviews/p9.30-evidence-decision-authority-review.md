# P9.30 Local Evidence Decision Authority Review

Status: pass for the bounded local evidence-decision slice.

## Decision

P9.30 passes. A Workbench user can record a human-only local acceptance or rejection for
one current verifier result. The decision changes work readiness according to all current
required pairs while leaving the proposal unapproved and unapplied and preserving source
and snapshot immutability.

## Acceptance Evidence

- the dedicated harness passes 19 positive checks
- 25 malformed, type-invalid, stale, duplicate, unsafe, or authority-promoted decisions fail
  zero-write
- 13 projection corruptions are rejected, including fabricated readiness, fabricated
  coverage, and incomplete decision-authority topology
- a persisted decision-derived evidence-record tamper is rejected by exact derivation
- 3 receipt/result/decision ordering probes pass
- concurrent duplicate decisions produce exactly one winner
- cross-operation contention preserves both records without a lost update
- loopback server smoke passes all 30 probes, including nested-array JSON rejection as
  a controlled `400` with zero writes
- P9.29 verifier-result regression passes 18 probes
- project, receipt, and stage regressions pass 8, 8, and 10 probes respectively
- deterministic static emission and validation pass with 8,208 nodes and 8,023
  relations
- repeated outputs are deterministic

## Authority Decision

- reviewer actor type: human only
- reviewer roles: `maintainer`, `quality-reviewer`, `security-reviewer`
- permissions: `evidence.accept`, `evidence.reject`
- authority scope: `local-project-workspace`
- cryptographic reviewer authentication: false
- acceptance of fail or blocked current result: forbidden
- decision over superseded result: forbidden
- proposal approval/application: false
- graph/source/snapshot/target mutation: false

## Readiness Semantics

Rejection blocks the work item. Partial acceptance remains `verification-observed`.
Only all current required pairs accepted with passing results move the work item to
`verified`. A newer verifier result does not inherit an older result's decision.

## Visual Review

The renderer reaches effective `100x` through renderer `24x` and virtual geometry
`4.1667`. Selected edges taper to `0.065px` at `0.34` opacity. Cached viewport-local
etched obsidian/titanium material with asymmetric spectral etching improves the dark
technical surface. Far-zoom material culling keeps ordinary code facts in the graph while
reserving the cached optical pass for structural and interaction landmarks. Nodes,
relations, source state, and graph history remain unchanged.

The automated server smoke validates emitted contracts and HTTP behavior. A separate live
in-app browser observation measured `8,208` nodes, `8,023` relations, logical zoom `100`,
renderer zoom `24`, effective geometry zoom `100`, selected-edge width `0.065`,
selected-edge opacity `0.34`, and zero console errors. At the fitted overview, 7,477
ordinary code materials were culled while every graph node remained loaded.

## Preserved Boundary

P9.30 is local evidence-decision authority only. It does not authenticate the reviewer,
run a verifier, upload evidence, approve a proposal, apply a graph delta or code diff,
mutate source or snapshot state, or grant productization/release authority.
