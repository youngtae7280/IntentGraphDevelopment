# P9.12 Experimental C# Fact-Only Workbench

Date: 2026-07-13

Decision: implementation and deterministic artifact gates pass. Browser visual and interaction confirmation remains pending because the available automation surface blocks newly opened local `file://` pages.

## Delivered Surface

`intentgraph emit-experimental-csharp-fact-workbench --workspace <workspace> --out <new-output-directory>` emits a separate local output directory without changing the input snapshot workspace or its external project.

The WindowsUtility reproduction output is:

```text
generated/windowsutility/experimental-csharp-fact-workbench/p9.12/index.html
```

It contains a dark Cytoscape-based graph inspector with a structural default view, search, fact/relation/source filters, pan/zoom/fit controls, resizable rails, selectable node and edge provenance panels, and an explicit list of semantic/change records not recorded in this fact-only workspace.

## Evidence

- The actual WindowsUtility snapshot output contains 8,137 code-fact nodes and 7,931 syntax-relation edges.
- Repeated exports of the same snapshot are byte-identical.
- The output validator confirms exact input provenance, endpoint integrity, node/edge inspector data, local bundled Cytoscape assets, explicit unavailable state, and false mutation/network/approval authority.
- Eleven repeatable negative probes reject unsafe input workspace state, source text/path leakage, semantic-resolution promotion, unknown facts, unresolved relations, output collision/overlap, external HTML runtime URLs, missing unavailable state, and graph-mutation authority promotion.
- B1 local-review initialization, validation, review, and logical-source negative probes still pass after the local facade extension.

## Boundary Preserved

- no target repository write, build, restore, launch, package dependency, network, provider, credential, code application, approval automation, graph mutation, or productization claim
- no source text, absolute external source path, resolved call graph, Intent mapping, proposal, graph delta, code diff, verification, evidence, acceptance authority, or history claim
- invocation facts and `invokes-syntax` edges remain visibly ambiguous syntax observations

## Remaining Visual Gate

P9.11 requires real browser evidence for a nonblank graph, pan/zoom, node click, edge click, and visible unavailable code-diff state. The local in-app browser automation rejected the new `file://` target under its URL policy. This gate is not passed by structural checks alone.

The generated local file must be opened and inspected before P9.12 can be treated as visually accepted. The next response should be concrete UI feedback, not an automatic promotion to generic C# support or C# Intent mapping.
