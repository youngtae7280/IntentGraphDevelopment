# P8.87 Artifact Signing Authorization Request

Work item: P8.87 Artifact Signing Authorization Request
Date: 2026-07-10
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: artifact signing authorization is requested, not recorded, and not actionable yet.

## Scope

P8.87 records the future artifact signing authorization request. It does not sign artifacts.

This request is not actionable until these pass:

- verified package or installer artifact evidence
- deterministic signing policy
- key/certificate authority boundary

Required accepted response:

```text
accept sandboxed artifact signing
```

Allowed responses:

- accept sandboxed artifact signing
- reject artifact signing
- revise signing scope first

The current-goal `accept/revise/blocked` shortcut applies to ordinary review gates only. It does not authorize signing, key access, credential access, timestamp authority calls, release publishing, or productization authority.

## Boundary Confirmation

Still false:

- existing package extraction performed
- packaged executable launched
- packaged UI launched
- packaged UI screenshot captured
- installer created
- global install performed
- artifact signed
- private key accessed
- certificate store accessed
- signing token accessed
- timestamp authority called
- credential accessed
- provider API called
- release published
- productization claimed

## Recommendation

Continue without execution to:

```text
P8.88 Artifact Signing Scope Hold Record
```
