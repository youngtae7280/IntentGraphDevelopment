# P1.6 B0 Typed Preservation Negative Probes Review

Work item: P1.6 Repeatable B0 Typed Preservation Negative Probe Harness
Date: 2026-07-09
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: continue

## Scope

P1.6 adds a repeatable negative-probe harness for B0 typed preservation metadata. The harness starts from the committed good `generated/b0-python-cli-calculator/calc.intentgraph.json`, creates mutated metadata copies in a temporary directory, runs `tools/retrofit_reconstruct.py`, and passes only when every mutated metadata case fails with the expected error.

This is a harness slice only. It does not expand typed preservation domains, remove `hiddenState.sourceGraphSnapshot`, claim code-only reconstruction, add dependencies, or touch CF0.

## Produced Artifacts

- `tools/run_b0_typed_preservation_negative_probes.py`
- `generated/b0-python-cli-calculator/p1.6-typed-preservation-negative-probes-report.json`

## Acceptance Criteria

| Criterion | Result |
|---|---|
| B0 positive generated-code pipeline still passes | Pass |
| every defined typed-preservation mutation causes retrofit failure | Pass |
| harness exits zero only when expected failures are observed | Pass |
| report is deterministic and inspectable | Pass |
| full snapshot remains present | Pass |
| no code-only reconstruction claim is added | Pass |
| no typed domain expansion is introduced | Pass |

## Negative Probes

The harness report records these probes with `expectedFailureObserved: true`:

- `missing-typed-preservation`
- `snapshot-still-present-false`
- `missing-domain-evidence`
- `stale-evidence-digest`
- `missing-evidence-record`
- `wrong-evidence-count`
- `unsorted-evidence-records`
- `missing-authority-record`
- `missing-history-record`
- `missing-intent-unit-record`
- `missing-unit-edge-record`

## Report Evidence

`generated/b0-python-cli-calculator/p1.6-typed-preservation-negative-probes-report.json` records:

- `result: pass`
- `probeCount: 11`
- each probe id, mutation, expected error substring, actual exit code, actual retrofit message, and `expectedFailureObserved`
- positive baseline typed preservation version `p1.5-typed-preservation-v0`
- positive baseline domains `authority`, `evidence`, `history`, `intentUnits`, and `unitEdges`
- boundary flags showing the harness did not remove the snapshot, claim code-only reconstruction, expand typed domains, or add dependencies

## P2 Decisions

- `P1.6-P2-001`: bad metadata fixtures are generated in temporary directories rather than committed. Decision: defer committed invalid fixtures because the harness report is deterministic and the script makes each mutation explicit.
- `P1.6-P2-002`: probes focus on representative typed domains and evidence-specific stale/count/order cases. Decision: defer exhaustive per-domain stale/count/order permutations until a larger harness is justified.

## Recommendation

Continue. A good next slice is either another small typed-domain expansion for B0 or a second code-first maintenance delta shape, depending on whether the Coordinator wants to keep reducing generated-code snapshot dependence or broaden ordinary maintenance evidence.
