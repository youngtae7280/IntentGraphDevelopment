# P8.44 Minimal WindowsUtility Source Edit Application

Work item: P8.44 Minimal WindowsUtility Source Edit Application
Date: 2026-07-10
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: minimal source edit applied and validated.

## Scope

P8.44 applies the low-risk WindowsUtility test-automation source edit selected in P8.43.

Applied file:

```text
tests/RegressionSmoke/Invoke-IntentGraphShellWorkspacePreflight.ps1
```

This file is a no-hardware preflight helper for shell/workspace source-application evidence. It does not touch product runtime code, Utility_Windows reference source, Native interop, hardware paths, packaging, or release paths.

## Application Result

WindowsUtility commit:

```text
ac5b2204442cde751f625a9979a4fdb437d468a8
Add IntentGraph shell workspace preflight
```

Push status:

```text
pushed to origin/main
HEAD == origin/main
```

## Validation

Final validation passed:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\RegressionSmoke\Invoke-IntentGraphShellWorkspacePreflight.ps1
dotnet build .\WindowsUtility.sln
```

Results:

- preflight passed
- build passed
- warnings: 0
- errors: 0
- WindowsUtility final status: clean/aligned

## Preview Deviation

The initial P8.43 preview script failed when applied because Windows PowerShell evaluated the default parameter expression before `$PSScriptRoot` was available, causing `Join-Path` to receive an empty path.

The correction stayed inside the same selected file and did not expand scope. `RepoRoot` resolution now happens in the script body and falls back to `$MyInvocation.MyCommand.Path`.

## Boundary Confirmation

True in P8.44:

- source edit exercised
- target write performed
- WindowsUtility commit created
- WindowsUtility push performed

Still false:

- AI authority promotion
- hardware actions
- product runtime behavior change
- Native interop change
- Utility_Windows mutation
- packaging
- release
- productization

## Produced Artifacts

- `generated/windowsutility/source-application-applications/p8.44/application-report.json`
- `generated/windowsutility/source-application-applications/p8.44/applied-Invoke-IntentGraphShellWorkspacePreflight.ps1`
- `generated/roadmap/p8.44-minimal-windowsutility-source-edit-application-report.json`

## Recommendation

Continue to:

```text
P8.45 Source Application Result Review / Next Productization Gate
```

P8.45 should decide whether this first real source application loop is enough to unblock the next productization gate, or whether another source application/evidence loop is required first.
