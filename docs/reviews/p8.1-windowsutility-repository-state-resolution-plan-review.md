# P8.1 WindowsUtility Repository State Resolution Plan

Work item: P8.1 WindowsUtility Repository State Resolution Plan
Date: 2026-07-10
Reviewer: IntentGraphDevelopment Roadmap Orchestrator

Decision: WindowsUtility adoption remains blocked until its repository state is explicitly resolved or scoped.

## Scope

P8.1 records how the selected real-project target state must be handled before any accepted mapping, target write, proposal application, or productization work.

This slice is report-only. It does not modify WindowsUtility, inspect untracked file contents, commit, push, clean, delete, apply patches, accept mappings, or authorize target writes.

## Observed Target State

Target:

```text
C:\Users\ytkim\Desktop\kyt_work\WindowsUtility
```

Observed status:

```text
## main...origin/main [ahead 4]
?? .devview/
```

Meaning:

- local `main` is ahead of `origin/main` by four commits.
- untracked `.devview/` exists.
- current state may contain user work or prior generated artifacts.
- IntentGraphDevelopment must not clean, rewrite, or treat this state as baseline without explicit acceptance.

## Resolution Options

| Option | Description | Safe next use |
|---|---|---|
| clean/aligned target | user commits/pushes or otherwise resolves target state before adoption | accepted mapping boundary may start |
| explicit dirty baseline | user explicitly approves the current target state as the baseline | read-only accepted mapping boundary may start with a dirty-baseline record |
| alternate target | choose another clean real-project target | return to real-project target selection |
| stay read-only | keep using current inventory/hypotheses only | no accepted mapping or target-write adoption |

## Required Precondition For Target Writes

Target writes remain blocked until a later slice records all of:

- repository state resolution decision
- before-state digest inventory
- rollback strategy
- accepted mapping boundary
- proposal validation boundary
- evidence requirements
- authority requirements
- user/coordinator approval for target writes

## Acceptance Criteria

| Criterion | Result |
|---|---|
| target state recorded | Pass |
| destructive cleanup avoided | Pass |
| target writes blocked | Pass |
| resolution options listed | Pass |
| accepted mapping remains blocked | Pass |
| productization remains blocked | Pass |

## Recommendation

The next safe work is either:

```text
P8.2 WindowsUtility Accepted Mapping Boundary Plan
```

if the Coordinator accepts a read-only mapping acceptance plan, or:

```text
external target-state resolution
```

if actual adoption must proceed against WindowsUtility.
